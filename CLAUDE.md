# CLAUDE.md

Поведенческие и проектные гайдлайны для работы с IndiaHorizone — tech-enabled India concierge для русскоязычных клиентов.

---

## Принципы разработки

**Компромисс:** Эти принципы смещены в сторону осторожности, а не скорости. Для тривиальных задач используй здравый смысл.

### 1. Думай перед кодом

**Не предполагай. Не скрывай путаницу. Озвучивай компромиссы.**

- Явно формулируй допущения. Если не уверен — спроси.
- Если есть несколько интерпретаций — представь их все, не выбирай молча.
- Если есть более простой подход — скажи об этом.
- Если что-то неясно — остановись. Назови, что именно непонятно.

### 2. Простота прежде всего

**Минимум кода, который решает задачу. Ничего спекулятивного.**

- Никаких фич сверх запрошенного.
- Никаких абстракций для одноразового кода.
- Никакой «гибкости», которую не просили.

Тест: «Сказал бы senior-инженер, что это переусложнено?» Если да — упрощай.

### 3. Хирургические изменения

**Трогай только то, что нужно. Убирай только свой мусор.**

- Не «улучшай» соседний код без запроса.
- Следуй существующему стилю.
- Каждая изменённая строка должна прослеживаться к запросу пользователя.

### 4. Целевое исполнение

**Определи критерии успеха. Итерируй до верификации.**

Трансформируй задачи в проверяемые цели:
- «Добавь форму» → «Форма валидирует поля X, Y, Z и отправляет на endpoint /api/...»
- «Почини баг» → «Напиши тест, воспроизводящий баг, затем исправь»

---

## О проекте

**IndiaHorizone** — нишевой travel-tech сервис для русскоязычных клиентов.

### Позиционирование

> Не турагентство. Не просто travel. А tech-enabled India concierge для русскоязычного клиента.

- **Продукт** = Индия без хаоса
- **Технология** = ускоряет продажи и сервис
- **Локальная сеть** = даёт реальную опору на месте
- **Сопровождение** = создаёт доверие и высокую ценность

### Модель развития

1. Кастомные туры и high-touch сервис
2. Автоматизация и повторные продажи
3. Масштаб через групповые форматы и тематические продукты

---

## Технологический стек (целевой)

| Слой | Стек |
|------|------|
| Фронтенд | Next.js 14 App Router + React 18 + TypeScript |
| Mobile | React Native / Expo |
| UI | Tailwind CSS + shadcn/ui |
| Backend | NestJS + Prisma |
| БД | PostgreSQL 15+ |
| Кеш | Redis 7+ |
| Файлы | S3-совместимое хранилище |
| Реальное время | WebSocket / Server-Sent Events |
| AI | OpenAI API (GPT-4o) |
| Уведомления | Telegram Bot API + Push (FCM/APNs) |
| Видео/фото | FFmpeg + S3 |
| Карты | Mapbox / OpenStreetMap |
| Оффлайн | PWA + Service Workers |
| Инфра | Docker Compose, GitHub Actions |
| Мониторинг | Grafana + Prometheus |

---

## Структура проекта (целевая)

```
indiahorizone/
├── apps/
│   ├── web/                  # Next.js клиентский портал
│   ├── mobile/               # React Native приложение (клиент + гид)
│   ├── admin/                # Панель управляющих
│   └── api/                  # NestJS backend
├── packages/
│   ├── ui/                   # Общие UI компоненты
│   ├── core/                 # Бизнес-логика (бронирования, маршруты, клиенты)
│   ├── notifications/        # Уведомления (Telegram, Push, Email)
│   └── ai/                   # AI-ассистент и LLM-интеграции
├── docs/                     # ТЗ, архитектура, роадмап
│   ├── ТЗ.md                 # Техническое задание (главный документ)
│   ├── ARCHITECTURE.md       # Архитектурные решения
│   └── ROADMAP.md            # Роадмап по этапам
├── scripts/                  # Деплой, миграции, утилиты
├── docker-compose.yml
├── README.md
├── CLAUDE.md
└── CONTRIBUTING.md
```

---

## Роли в системе

| Роль | Описание |
|------|----------|
| `client` | Клиент — путешественник, основной пользователь |
| `guide` | Гид / локальный оператор в Индии |
| `manager` | Менеджер по продажам |
| `concierge` | Concierge — поддержка клиентов 24/7 |
| `finance` | Финансы / бухгалтер |
| `admin` | Управляющий / основатель |

---

## Ключевые модули

### Клиентский кабинет (trip dashboard)
- Документы и маршрут поездки (оффлайн-доступ)
- Таймлайн дня: «сейчас / следующее»
- Ежедневный фидбэк (текст / видео-кружок)
- SOS-кнопка и экстренные контакты
- AI-ассистент с контекстом маршрута

### CRM (менеджеры)
- Карточка клиента: история, предпочтения, документы
- Воронка продаж и статусы
- Коммуникация (чат, звонки, история)
- Квиз и онбординг клиента

### Операционная панель (гиды)
- Расписание и список клиентов дня
- Чат с клиентом и concierge
- Загрузка фото/видео → в альбом клиента
- Учёт расходов и сверка

### Аналитика (управляющие)
- Воронка, CAC, LTV, маржа
- NPS по гидам, менеджерам, маршрутам
- Прогноз выручки

---

## Git-воркфлоу

Ветки:
```
feature/описание-номер-issue
fix/описание-номер-issue
docs/описание-номер-issue
```

Коммиты — Conventional Commits:
```
feat(scope): описание
fix(scope): описание
docs(scope): описание
refactor(scope): описание
test(scope): описание
chore(scope): описание
```

Примеры:
```
feat(client): добавить оффлайн-режим trip dashboard
fix(sos): исправить экстренный вызов на iOS
docs(tz): обновить ТЗ модуля ежедневного фидбэка
```

---

## Стандарты кода

### TypeScript

- Строгая типизация, явные интерфейсы
- Не использовать `any` без крайней необходимости
- Именованные экспорты

### React-компоненты

- Функциональные компоненты с `FC<Props>`
- Интерфейс пропсов рядом с компонентом
- Доступность: `aria-label`, семантические теги

### Мобильное приложение

- Оффлайн-first: все критичные данные кешируются локально
- Учитывать нестабильный интернет в Индии (retry, graceful degradation)
- Поддержка iOS и Android

---

## Безопасность

- Персональные данные клиентов — GDPR / 152-ФЗ
- Документы (паспорта, визы) — шифрование at rest и in transit
- Роли и права — RBAC, минимум привилегий
- Никогда не коммитить `.env`, ключи API, credentials
- Аудит-лог всех критичных действий

---

## Переменные окружения

```bash
DATABASE_URL=          # PostgreSQL
REDIS_URL=             # Redis
JWT_SECRET=            # Авторизация
OPENAI_API_KEY=        # AI-ассистент
TELEGRAM_BOT_TOKEN=    # Уведомления
S3_BUCKET=             # Хранилище файлов
S3_ACCESS_KEY=
S3_SECRET_KEY=
MAPBOX_TOKEN=          # Карты
FCM_KEY=               # Push-уведомления Android
APNS_CERT=             # Push-уведомления iOS
```

---

## Принципы продукта

1. **Оффлайн-first** — клиент в Индии не должен зависеть от стабильного интернета
2. **Mobile-first** — основные сценарии проектируются для телефона
3. **Русскоязычный UX** — весь интерфейс на русском, без английских вставок
4. **Доверие через прозрачность** — клиент всегда знает, что происходит и что будет дальше
5. **Минимум шагов** — любое действие (SOS, фидбэк, запрос доп. услуги) — максимум 2 тапа
